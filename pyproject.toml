[project]
name = "vtu-auto"
version = "0.1.78"
description = "VTU online video progress automater"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
dependencies = [
    "requests>=2.33.1",
    "rich>=13.0.0",
]

[project.scripts]
vtu-auto = "vtu_auto.cli:main"
